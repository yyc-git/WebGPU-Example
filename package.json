{
    "name": "webgpu-example",
    "private": true,
    "scripts": {
        "clean": "lerna clean",
        "install": "lerna bootstrap"
    },
    "workspaces": [
        "packages/*"
    ],
    "config": {
        "commitizen": {
            "path": "node_modules/cz-customizable"
        }
    },
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "dependencies": {
        "@webgpu/types": "^0.1.2",
        "gl-matrix": "^3.2.1",
        "typescript": "^3.8.3"
    }
}